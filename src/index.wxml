<view class="scroll-wraper custom-class">
    <scroll-view id="scroll-view" class="scroll-box" scroll-y="true" style="height: {{height}};" bindscrolltoupper="upper" bindscrolltolower="lower">

        <block wx:for="{{list}}" wx:key="index">
            <item item="{{item}}" binddelItem="deleteItem"></item>
        </block>

        <slot name="loading" wx:if="{{loading}}"></slot>
        <slot name="loading-finish" wx:else></slot>
        <view class="loading" wx:if="{{!$slots['loading']}}">
            {{loading ? '加载中...' : '到底了'}}
        </view>

        <block wx:if="{{!list.length && !loading}}">
            <view class="empty" wx:if="{{!$slots['empty']}}">
                <image src="https://img01.yzcdn.cn/vant/custom-empty-image.png" mode="aspectFill" class="icon-empty" mode="aspectFill"></image>
                <view class="empty-text">暂无数据</view>
            </view>
            <slot name="empty"></slot>
        </block>
    </scroll-view>
</view>

<!-- <view class="container">
  <virtual-list scrollToIndex="{{ 16 }}" lowerThreshold="{{50}}" height="{{ 600 }}" 
  overscanCount="{{10}}" item-count="{{ itemCount }}" itemSize="{{ itemSize }}" 
  estimatedItemSize="{{100}}" bind:render="slice" bind:scrolltolower="loadMore">
    <view wx:if="{{styleItems}}">
      <view wx:for="{{ list }}" wx:key="index" style="{{ styleItems[index] }};
      line-height:50px;border-bottom:1rpx solid #ccc;padding-left:30rpx">{{ item + 1 }}</view>
    </view>
  </virtual-list>
  {{itemCount}}
</view> -->

